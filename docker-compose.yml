services:
  web:
    build:
      context: .
      dockerfile: go/Dockerfile
    container_name: cnqso-web-server
    ports:
      - "127.0.0.1:1739:1738"
    environment:
      - CNQSO_PORT=:1738
      - CNQSO_UPLOAD_DIR=/app/uploads
      - CNQSO_LIBRARY_DIR=/app/files
      - CNQSO_COMPILE_TYPESCRIPT=false
      - CNQSO_FINGERPRINTING=${CNQSO_FINGERPRINTING:-true}
      # Nginx reaches the container through Docker's bridge gateway.
      - CNQSO_TRUSTED_PROXIES=${CNQSO_TRUSTED_PROXIES:-127.0.0.1/32,::1/128,172.16.0.0/12}
    restart: unless-stopped
    stop_grace_period: 30s

    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "2.0"
        reservations:
          memory: 128M
          cpus: "0.25"

    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:1738/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    volumes:
      - ./webm:/app/uploads
      - ./go/db:/app/db
      - ./go/files:/app/files

    tmpfs:
      - /tmp
